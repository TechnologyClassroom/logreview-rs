# Configuration

logreview-rs only accepts configuration files in the TOML format.

`config.toml` is the iteration that I am using for development.

I plan to put example files for common log formats here.
